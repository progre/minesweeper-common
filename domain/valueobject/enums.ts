export enum Landform {
    NONE,
    BOMB,
    UNKNOWN
}

export enum Status {
    CLOSE,
    OPEN,
    UNKNOWN
}

export enum Layer {
    NONE,
    FLAG,
    QUESTION,
    UNKNOWN
}

export enum Intent {
    MOVING, DIGGING, FLAG, QUESTION, REMOVE_QUESTION
}
